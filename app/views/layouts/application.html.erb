<!DOCTYPE html>
  <html>
    <head>
      <title>Bbq</title>
      <meta name="viewport" content="width=device-width,initial-scale=1">
      <%= csrf_meta_tags %>
      <%= csp_meta_tag %>

      <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
      <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
      <script src="https://api-maps.yandex.ru/2.1/?apikey=<%= ENV['MAP_API_KEY'] %>&lang=ru_RU" type="text/javascript"></script>
    </head>

    <body>
      <div class="container">
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
          <div class="container-fluid">
            <%= link_to t('app.name'), root_path, class: 'navbar-brand' %>
             <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                  
              <% if user_signed_in? %>
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <li class="nav-item">
                    <%= link_to t('app.profile'), current_user, class: 'nav-link active', data: { turbo: false }, aria: { label: "page" } %>
                  </li>
                  <li class="nav-item">
                    <%= link_to t('app.exit'), destroy_user_session_path, class: 'nav-link active', data: { turbo_method: :delete }, aria: { label: "page" } %>
                  </li>
                </ul>
              <% else %>
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <li class="nav-item">
                    <%= link_to t('app.sign_in'), new_user_session_path, class: 'nav-link active', aria: { label: "page" } %>
                  </li>
                  <li class="nav-item">
                    <%= link_to t('app.sign_up'), new_user_registration_path, class: 'nav-link active', aria: { label: "page" } %>
                  </li>
                </ul>
              <% end %>

              <ul class="navbar-nav mb-2 mb-lg-0">                     
                <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <%= t('app.language') %>
                  </a>
                  <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <li><%= link_to t('app.english'), url_for(back: true, locale: :en), class: "dropdown-item" %></li>
                    <li><%= link_to t('app.russian'), url_for(back: true, locale: :ru), class: "dropdown-item" %></li>
                  </ul>
                </li>        
              </ul>
            </div>
          </div>
        </nav>
      </div>

      <div class="container">
        <div class="row">
          <div class="col-lg-12 mt-3">
            <%= bootstrap_flash %>
            <%= yield %>
          </div>
        </div>
        <footer class="sticky-bottom py-3 text-center bg-light">
          <div class="text-center p-3" style="background-color: rgba(0, 0, 0, 0.2);">
            &copy; <%= t('app.company') %>
          </div>
        </footer>
      </div>
    </body>
  </html>
